{
    "version": "0.1.0",
    "name": "Python",
    "id": "python",
    "description": "Installs Python and Poetry.",
    "options": {
        "releaseCycle": {
            "type": "string",
            "default": "automatic",
            "proposals": [
                "latest",
                "automatic"
            ],
            "description": "The Python release cycle to install. Can be a Major.Minor version, 'automatic' to use the default version for the image, or 'latest' to use the latest available version."
        }
    },
    "installsAfter": [
        "ghcr.io/froazin/devcontainer-features/sdk:0"
    ]
}
