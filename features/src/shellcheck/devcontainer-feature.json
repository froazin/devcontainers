{
    "version": "0.1.1",
    "name": "ShellCheck",
    "id": "shellcheck",
    "description": "A static analysis tool for shell scripts.",
    "options": {
        "shellcheckVersion": {
            "type": "string",
            "default": "latest",
            "description": "The version of shellcheck to install."
        },
        "shellcheckBinPath": {
            "type": "string",
            "default": "automatic",
            "description": "The path to the shellcheck binary."
        }
    },
    "installsAfter": [
        "ghcr.io/froazin/devcontainer-features/sdk:0"
    ]
}
