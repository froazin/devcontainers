{
    "name": "base-ubuntu",
    "scripts": {
        "build": "npx @devcontainers/cli build --workspace-folder . --image-name ghcr.io/froazin/devcontainer-images/base:noble"
    },
    "devDependencies": {
        "@devcontainers/cli": "^0.75.0"
    }
}
